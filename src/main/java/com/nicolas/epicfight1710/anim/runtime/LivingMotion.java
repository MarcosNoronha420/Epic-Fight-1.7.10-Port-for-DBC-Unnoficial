package com.nicolas.epicfight1710.anim.runtime;

/** A 1.7.10 subset of Epic Fight's LivingMotion registry. */
public enum LivingMotion {
    IDLE, WALK, RUN, SNEAK, JUMP, FALL, LANDING, FLOAT, FLY, CREATIVE_IDLE, FLY_FORWARD, FLY_BACKWARD,
    CLIMB, SWIM, MOUNT, SIT, SLEEP, KNEEL, DEATH
}
